Unicode proposal to add missing PETSCII characters.

# Files

`table.txt`: A table of all PETSCII characters

`new.txt`: A subset of `table.txt` containing all the characters that don’t yet
exist in Unicode.
